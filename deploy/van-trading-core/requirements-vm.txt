# van-trading-core runtime (Python 3.12, Ubuntu 24.04 arm64). Pins are exact; the adoption gate
# for each is recorded in trading/architecture/stack_lock.json and the Rev 5 blueprint.
fastapi==0.141.1
uvicorn==0.53.0
httpx==0.28.1
psycopg[binary]==3.2.9
websockets==17.1
pydantic>=2.7,<3
pyyaml>=6.0,<7
# --with-nautilus installs the kernel donor at the Phase 3 gate (manylinux_2_35 aarch64 wheel verified on PyPI)
# nautilus_trader==1.231.0

temporalio==1.33.0
