# CURSOR PATCH PROMPT — VAN AURA SEMANTIC ENVELOPE / TRADE-STATE READY CORRECTION

You are not starting from scratch.
You are patching the improved VAN implementation because the aura is still too tight to the body and still does not provide enough semantic bandwidth for future trade-state signalling.

Your job is to surgically correct the current implementation so the aura becomes a **three-zone system** with a larger outer semantic field.

---

## 1. PRIMARY PATCH OBJECTIVE

Correct the current aura so it no longer behaves mainly as a body-hugging decorative system.

Implement:

1. **Inner Presence Field**
2. **Mid Interaction Field**
3. **Outer Semantic Envelope**

The missing piece is the **Outer Semantic Envelope**.

This outer semantic layer must:
- sit farther from the body than the current arcs;
- carry restrained semantic colour;
- support warning/error/urgent/attention states more clearly;
- support future trade-state semantics without recolouring VAN's body;
- remain readable in reduced motion and low/static effect budgets.

---

## 2. WHAT IS STILL WRONG RIGHT NOW

The current visuals are improved, but still flawed because:

- the aura remains too tight to the character silhouette;
- there is not enough spatial breathing room for semantic meaning;
- many states still rely too much on icons and lower glass strips;
- the current arcs are still too similar in radius and proximity across states;
- future trade-state colours would have nowhere premium to live except on the body or in icons;
- reduced motion / low effect budgets still compress too much of the state-bearing field.

Do not ignore these. This patch exists specifically to solve them.

---

## 3. REQUIRED AURA ARCHITECTURE

### Zone A — Inner Presence Field
Keep this close to VAN.

Purpose:
- preserve identity;
- separate the silhouette from the background;
- keep a stable DIAL cyan/blue body-adjacent glow.

Rules:
- mostly cyan/blue;
- low semantic contamination;
- close to head/torso/orb.

### Zone B — Mid Interaction Field
This is the current operational layer.

Purpose:
- filaments;
- working arcs;
- visor/orb/task coupling;
- activity-state expression.

Rules:
- asymmetrical;
- activity-bearing;
- still relatively near VAN;
- not the main semantic colour carrier.

### Zone C — Outer Semantic Envelope
This is the new required layer.

Purpose:
- semantic meaning;
- restrained state colour;
- future trade-state signalling;
- readable outer-field topology.

Rules:
- sparse;
- broken;
- non-circular;
- not a full ring;
- farther from the body;
- elegant, not noisy.

---

## 4. GEOMETRY PATCH

Do NOT just scale the whole aura up.

Instead:

- keep Zone A approximately at current inner scale;
- keep Zone B moderately larger than Zone A;
- create Zone C at roughly **1.35x to 1.70x** the current effective arc radius;
- maintain a visible gap between Zone B and Zone C.

The viewer must be able to perceive that Zone C is a separate semantic layer.

---

## 5. COLOUR PATCH

You must separate identity colour from semantic colour.

### Identity colour
- stays near the body
- primarily DIAL cyan/blue
- used by Zone A and mostly by Zone B baseline behaviour

### Semantic colour
- lives primarily in Zone C
- can be amber / red / green / violet / gold etc. depending on state
- must not repaint the whole character or whole inner aura

This separation is mandatory for future trade-state scalability.

---

## 6. STATE TOPOLOGY PATCH

Do not merely recolour states.
Every state must gain more distinct field topology via Zone C.

At minimum, adjust these states:

### WAITING_FOR_OWNER
- amber semantic envelope fragments
- aura should look like it is holding the approval context
- reduce dependence on lower strip alone

### WARNING
- amber caution bracket/semantic lane
- more distinct than a warning icon alone

### ERROR
- red interrupted semantic contour
- stronger failure topology than warning

### URGENT
- sharper, more direct red semantic brackets
- more focused and deliberate than error

### SUCCESS
- green confirmation/release arc
- elegant, not loud

### WORKING
- stronger mid interaction + fuller support from outer semantic envelope

### LISTENING / THINKING / SEARCHING
- give each a different outer semantic geometry, not only different props

### DEGRADED
- diminished / partially broken outer semantic envelope
- still readable and premium

### OFFLINE / SLEEPING
- very low semantic-envelope presence
- preserve distinction through tone and posture

---

## 7. TRADE-STATE READY PATCH

Design Zone C so it can later support market/trade states such as:
- monitoring / watchlist
- setup forming
- entry opportunity
- in-trade
- target hit / profit
- risk rising
- stop / invalidation
- urgent intervention

You do NOT need to fully implement a trading product now unless the repo already models it.
But you MUST make the aura architecture capable of supporting these later.

That means:
- do not spend all semantic capacity on near-body cyan arcs;
- reserve outer semantic space for future meanings.

---

## 8. REDUCED MOTION / LOW / STATIC PATCH

### Reduced motion
Preserve Zone C in static form.
Do not collapse semantics onto the body.

### LOW budget
Preserve at least one readable Zone C fragment.

### STATIC
Preserve:
- inner presence;
- one or more semantic envelope fragments;
- state readability;
- identity/semantic separation.

If low/static removes the outer semantic field completely, the patch fails.

---

## 9. BOARD / EVIDENCE PATCH

Revise the existing boards:

1. **18 durable states**
2. **reduced motion**
3. **low effect budget**

For each board:
- increase aura breathing room;
- make the outer semantic envelope visible;
- ensure state topology is more varied.

Also create a new board if possible:

**`VAN aura topology & semantic ladder`**

This board should show:
- Zone A
- Zone B
- Zone C
- FULL / REDUCED / REDUCED_MOTION / LOW / STATIC comparisons
- example semantic colour behaviours
- future trade-state readiness

---

## 10. HARD PROHIBITIONS

Do NOT:
- simply enlarge current arcs and call it solved;
- add a full circular ring;
- flood the whole aura/body with warning/urgent colours;
- introduce noisy clutter or too many segments;
- rely mainly on icon + bottom strip for semantics;
- let reduced-motion or low/static remove Zone C entirely.

---

## 11. PATCH ACCEPTANCE GATES

Your patch succeeds only if:

1. the aura no longer reads as tightly wrapped around the body;
2. there is visibly more semantic breathing room;
3. Zone C is visibly separate from the inner/mid field;
4. warning/error/urgent/waiting-for-owner are more readable through field shape and placement;
5. future trade-state colours can logically live in Zone C;
6. reduced-motion and low/static keep enough semantic field to remain clear;
7. the system still feels restrained, premium, and coherent.

---

## 12. REQUIRED WORKFLOW

1. inspect the current improved implementation;
2. identify exactly where the current aura geometry is defined;
3. patch the tokens/renderers/composables so the outer semantic envelope is real;
4. revise affected state boards/previews;
5. generate fresh evidence;
6. self-critique against the acceptance gates.

Do not stop after code edits. Stop only after the visual evidence reflects the semantic-envelope architecture.

Begin now by patching the aura system around the three-zone model and producing updated evidence boards.
