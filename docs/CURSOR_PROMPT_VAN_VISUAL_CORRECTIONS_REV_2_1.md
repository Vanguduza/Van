# CURSOR IMPLEMENTATION PROMPT — VAN VISUAL CORRECTIONS / REV 2.1

You are the lead visual-systems engineer, Android UI engineer, motion-system integrator, and acceptance authority for VAN.

Your task is not to write another plan or produce another loose mockup.

Your task is to **implement the VAN visual corrections deterministically** so the resulting visuals match the Rev 2.1 master blueprint and correct the failures in the current preview implementation.

You must continue until the implementation, visual evidence, and acceptance artifacts are all complete to the maximum extent possible inside the available repository/environment.

---

## 1. PRIMARY OBJECTIVE

Correct VAN's visuals so that the system is perceived as:

**VAN → living refractive aura → aura condenses into glass → glass carries information/actions**

and not as:

**character + circular background + tinted rounded rectangle + glowing border**

The implementation must preserve VAN's identity and correct the aura, glass, overlay composition, docking, state boards, reduced-motion behaviour, and Command Centre.

---

## 2. REQUIRED SOURCE MATERIALS TO READ FIRST

Read and reconcile these files before making changes:

1. `VAN_VISUAL_PRODUCTION_SYSTEM.md`
2. `VAN_VISUAL_DESIGN_CORRECTION_BLUEPRINT_REV_2.md`
3. `VAN_VISUAL_PRODUCTION_SYSTEM_REV_2_1_MASTER_BLUEPRINT.md`  ← this is the main implementation authority
4. `van-visual-authority-v1.yaml`
5. any existing `van-visual-authority-v1.1-release-manifest.json`
6. any visual authority boards / acceptance boards / preview assets
7. the current Android/Compose/Rive implementation files for VAN
8. the preview images showing the current wrong implementation

You must not start coding blind.

---

## 3. NON-NEGOTIABLE IDENTITY LOCKS

Do NOT redesign VAN's core identity.

Preserve:
- silver/white swept layered asymmetric hair;
- expressive visible eyes;
- continuous cyan/blue translucent visor band;
- medium-brown skin;
- charcoal technical undersuit;
- white/silver technical jacket;
- DIAL cyan/blue accents;
- cyan holographic orb companion;
- compact inventor-engineer silhouette;
- warm, calm, capable, professional personality.

This task is about correcting the visual system around the identity, not changing the identity.

---

## 4. CURRENT IMPLEMENTATION FAILURES YOU MUST FIX

The current implementation is wrong in these ways:

1. the aura reads as a cyan circular/elliptical background plate;
2. the aura edge reads like a border;
3. the glass reads as a dark translucent rounded rectangle with a glowing stroke;
4. floating minimal wrongly has a permanent small card;
5. expanded panels feel attached beside VAN, not generated by VAN;
6. docked mode looks accidentally clipped instead of intentionally docked;
7. state cards are too repetitive and too dependent on one rounded-square silhouette;
8. state glyphs look pasted on rather than arising from visor/orb/hologram systems;
9. reduced-motion looks like a diminished version instead of a designed still visual mode;
10. Command Centre looks like a generic placeholder dashboard;
11. effect-budget degradation collapses too much of the visual quality;
12. all 18 durable states are not fully represented in current evidence;
13. character scale/crop consistency drifts across states.

All of the above must be corrected.

---

## 5. MANDATORY VISUAL THESIS

Implement this exact thesis:

> VAN is a solid expressive character surrounded by a living refractive field.  
> When interface space is needed, that field condenses into glass.

This means:
- the aura and glass are one family;
- the character is the source of the UI;
- the UI must visually emerge from VAN;
- colour alone must never carry state.

---

## 6. DELIVERABLES

You must deliver the following, to the maximum extent possible in the repository:

### A. Implementation
- corrected floating minimal overlay
- corrected compact actions surface
- corrected expanded/working surface
- corrected docked edge mode
- corrected Command Centre
- corrected state-board previews
- reduced-motion variants
- low/static effect-budget variants
- token / rendering support for aura and glass V2

### B. Evidence
- screenshot/golden set for all required scenarios
- full 18-state board
- reduced-motion board
- low/static board
- glass material bench
- floating mode storyboard
- Command Centre evidence

### C. Documentation
- implementation notes
- changed files inventory
- evidence summary
- residual gaps, if any

---

## 7. REQUIRED WORKING METHOD

Follow this sequence.

### Step 1 — Inventory
Inspect the repo and create a concise implementation inventory:
- where VAN tokens are defined
- where aura is rendered
- where glass surfaces are rendered
- where overlay modes are implemented
- where docking is implemented
- where Command Centre is implemented
- how Rive is integrated
- how states/actions are mapped

Do not guess. Use actual file inspection.

### Step 2 — Gap map
Map the current implementation against Rev 2.1:
- what already exists
- what is partially correct
- what is wrong
- what must be replaced
- what can be extended

### Step 3 — Token / authority upgrade
Create or extend the authority/tokens needed for:
- aura V2
- glass V2
- effect budgets
- reduced-motion behaviour
- presentation mode material variants

### Step 4 — Aura rewrite
Replace the circular-background aura with a layered system:
- core radiance
- deformable outer field
- filaments
- broken orbital arcs
- caustic/specular interactions
- controlled bloom

A full circular ring is forbidden.

### Step 5 — Glass rewrite
Replace the “filled card + uniform stroke” model with layered glass:
- absorptive tint
- shaping gradient
- fine grain
- inner highlight
- structural edge
- selective specular edge
- aura contamination light
- shadow
- optional blur path
- fallback path

### Step 6 — Overlay behaviour
Make resting floating VAN frameless.
No permanent card at rest.

When interaction is required:
- form glass from the aura;
- make VAN overlap/break the glass edge;
- ensure the panel feels generated by VAN.

### Step 7 — Dock behaviour
Redesign dock mode so it looks intentional:
- preserve face/visor identity
- use deliberate edge anchor
- use crescent/edge aura
- avoid accidental-looking clipping

### Step 8 — Command Centre
Rebuild the visual hierarchy:
- hero VAN
- state / mission
- attention
- decisions
- tasks
- projects
- connections
- embedded solid approval actions

Remove placeholder/demo-looking card content from acceptance evidence.

### Step 9 — State boards
Produce all 18 durable states.
Normalize framing, scale, and aura origin.
Also produce:
- reduced-motion board
- low/static board

### Step 10 — Evidence and QA
Generate screenshots/goldens and self-critique them against the acceptance gates.

Do not stop at “implemented.”
Stop only when the evidence looks correct.

---

## 8. HARD CONSTRAINTS

### You MUST preserve:
- character identity
- state/action truth
- accessibility
- readability over arbitrary backgrounds
- reduced-motion support
- effect-budget fallbacks
- platform realism

### You MUST NOT:
- redesign VAN's face/hair/visor identity
- keep the aura as a circle
- keep the glass as a glowing-stroke card
- leave the resting floating mode attached to a permanent card
- use placeholder UI content in acceptance visuals
- rely on colour alone for state
- make warning/error frantic
- use a full-ring loader behind VAN
- ship half-clipped docked visuals that look accidental

---

## 9. STATE AND ACTION CONTRACT — MUST BE COMPLETE

### Durable states
Implement and evidence all 18:

OFFLINE  
CONNECTING  
IDLE  
ATTENTIVE  
LISTENING  
THINKING  
SEARCHING  
WORKING  
DELEGATING  
SPEAKING  
WAITING  
WAITING_FOR_OWNER  
DEGRADED  
WARNING  
ERROR  
SUCCESS  
URGENT  
SLEEPING

### Actions
Implement and reconcile all 14:

HELLO_WAVE  
ACK_NOD  
POINT_LEFT  
POINT_RIGHT  
POINT_UP  
POINT_DOWN  
POINT_TARGET  
CELEBRATE  
CAUTION  
CONFIRM  
SHRUG  
PRESENT_CARD  
OPEN_PANEL  
CLOSE_PANEL

---

## 10. REQUIRED ACCEPTANCE GATES

Your work fails if any of these fail.

### Gate A — No-circle aura
If the aura still first reads as a cyan circle/ellipse, fail.

### Gate B — No-stroke glass
If the glass still reads as a dark rounded rectangle with a glowing border, fail.

### Gate C — Character-first
If the card chrome is more visually dominant than VAN, fail.

### Gate D — Background resilience
If readability collapses over bright, dark, or busy backgrounds, fail.

### Gate E — Grayscale state clarity
If warning/error/success/urgent/waiting-for-owner are not distinguishable in grayscale by more than colour, fail.

### Gate F — Dock intention
If docked mode looks clipped rather than intentional, fail.

### Gate G — Reduced-motion quality
If reduced-motion looks like a dead/incomplete version rather than a designed variant, fail.

### Gate H — Full parity
If any durable state or action is missing, fail.

### Gate I — Command Centre maturity
If the Command Centre still looks generic or placeholder-like, fail.

---

## 11. ENGINEERING IMPLEMENTATION EXPECTATIONS

If the repo supports it, structure the implementation cleanly:

- tokens or authority definitions separate from renderer logic
- aura renderer separate from glass renderer
- overlay mode logic separate from visual styling
- shared visual state object
- no one-off hacks per screen
- no duplicated per-state styling logic scattered through many files

Prefer deterministic architecture.

If the repo is Android/Compose-based, logically separate concerns such as:
- `VanVisualTokensV2`
- `VanAuraRenderer`
- `VanGlassRenderer`
- `VanOverlaySurface`
- `VanDockBehavior`
- `VanCommandCentreScreen`
- state-board preview/evidence generation

Adapt names to the repo, but keep the architectural separation.

---

## 12. PLATFORM REALISM

Do not pretend true backdrop refraction is universally available.

Support three material paths:

1. native blur/refractive path when actually available
2. overlay-safe optical glass path as the main default
3. opaque fallback path

The design must still look premium in all three.

Do not use host-screen capture to fake refraction.

---

## 13. REQUIRED OUTPUT FORMAT FROM YOU (THE AGENT)

When you work, produce progress in this shape:

### Phase report structure
1. **Inventory**
2. **Gap analysis**
3. **Implementation plan**
4. **Files changed**
5. **Why each change was made**
6. **Evidence generated**
7. **Self-critique against acceptance gates**
8. **Residual gaps**
9. **Next actions**

At completion, provide:
- a final changed-files summary
- a screenshot/evidence manifest
- acceptance-gate pass/fail summary
- any external-blocker notes
- what remains only if the environment truly prevents completion

---

## 14. DEFINITION OF DONE

You are done only when:

- the aura is fluid, asymmetric, and state-reactive;
- the glass is layered and optical;
- resting floating VAN is frameless;
- compact/expanded panels clearly grow from VAN's aura;
- VAN visibly breaks/intersects the panel edge;
- docked mode is deliberate and polished;
- all 18 states and 14 actions are implemented and evidenced;
- reduced-motion is designed and premium;
- Command Centre is mature and operationally structured;
- screenshot evidence passes the acceptance gates.

If the result still reads like “character inside a dark rounded card with a glowing border,” you have not completed the task.

Now begin by reading the authority files, inventorying the current implementation, and producing a precise gap analysis before editing code.
