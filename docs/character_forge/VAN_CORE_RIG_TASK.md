# VAN Core Rig Task — M2 Execution View

Authority remains `VAN_CHARACTER_FORGE_DEVELOPMENT_PACK_REV_2.md`; this is a derived task sheet.

Produce one Rive artboard named **Van** with state machine **VanRuntime**. Do not rename,
add or remove public inputs/triggers. Freeze the skeleton only after owner PASS on the S24.

Core scope only: IDLE (2), LISTENING (4), THINKING (5), SPEAKING (9); HELLO_WAVE (1),
ACK_NOD (2), POINT_TARGET (7); gaze, random blink, mouth_open + visemes 0–4, breathing,
orb idle drift. The artboard is transparent and contains no Android aura.

Export under `visual-authority/character-forge/09-rive-working/`, then record a packaging
receipt and stage it with the Forge CLI. A core PASS is not accepted without a green
android-instrumentation run, downloaded frame evidence, and the owner's real-device verdict.
