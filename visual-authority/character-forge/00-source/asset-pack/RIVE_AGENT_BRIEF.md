# VAN Rive Agent Build Brief — Approved Authority Pack

Build VAN from this pack without visual invention.

## Read first

`APPROVED_IDENTITY_LOCK.yaml`, `ASSET_PACK_MANIFEST.yaml`, `LAYER_SPEC.yaml`,
`RIG_SPEC.yaml`, `ANIMATION_SPEC.yaml`, `STATE_ACTION_MATRIX.yaml`,
`SPEECH_SPEC.yaml`, `AURA_HANDOFF_SPEC.yaml`, `VALIDATION_MATRIX.yaml`.

The sole primary image authority is
`visual-authority/assets/pack/owner_board_visual_authority.png`.

## Never use

Any path in `LEGACY_ASSET_DENYLIST.yaml`, screenshots of the rejected light-skinned/headband VAN,
procedural placeholder sheets, or derived crops created from them.

## Identity invariants

Medium-brown skin; silver/white swept hair; blue eyes; cyan transparent visor; black/white technical
jacket; charcoal underlayer; black gloves; DIAL cyan accents; cyan holographic orb; no headband;
approximately 5.75-head proportions. Character body remains opaque.

## Rive boundary

Artboard `Van`, state machine `VanRuntime`, exactly the public inputs/triggers in
`rive_contract.json`. The Android-native living aura is never placed in the Rive file.

## M1

Author `06-vectors-clean/van_layers.svg` with all required root groups and M2 extra groups. Do not
auto-trace a composite into flat geometry. Run SVG lint, render the layer sheet, and stop for
independent review.

## Capability probe

Before premium rigging, prove bones, weighted meshes, IK/transform constraints, blend states,
multiple state-machine layers, clipping and easing in committed RML. If a required feature is not
expressible, stop and record the hybrid-Editor decision instead of degrading the motion.

## M2 core

Implement IDLE/LISTENING/THINKING/SPEAKING; HELLO_WAVE/ACK_NOD/POINT_TARGET; gaze; blink; speech;
breathing; orb drift. Expose the full public contract from the beginning. Build, receipt, stage,
validate, generate motion evidence, then stop for the owner core verdict. Freeze the skeleton after PASS.

## M3 full

Add all 18 durable states and 14 finite actions without changing the frozen skeleton. `action_code`
is authoritative and actions latch to completion. Speech remains additive. Generate WebM/contact-sheet
evidence and independent review.

## Drift stops

Stop on any identity conflict, denylisted input, public-contract change, headband, light/peach skin,
dark hair, ungloved hands, chibi proportion collapse, body translucency, aura inside Rive, or need to
invent a new VAN identity feature.
