"""Owner-visible projection of the DIAL Jev service.

The VAN gateway never carries TypeSafe credentials. It reads only the private DIAL Jev
control-plane API and exposes an owner-device projection.
"""
